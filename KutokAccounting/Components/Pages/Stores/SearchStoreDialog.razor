<MudDialog>
    <TitleContent>
        Пошук
    </TitleContent>
    <DialogContent>
        <MudForm @bind-IsValid="_isValid">
            <MudTextField
                T="string"
                Label="Назва"
                Value="@StoreSearchParameters?.Name"
                Margin="Margin.Normal"
                ValueChanged="OnNameChanged"/>
            <MudTextField
                T="string"
                Label="Aдреса"
                Value="@StoreSearchParameters?.Address"
                Margin="Margin.Normal"
                ValueChanged="OnAddressChanged"/>
            <MudCheckBox
                Value="@StoreSearchParameters.IsOpened.GetValueOrDefault()"
                Color="Color.Primary"
                UncheckedColor="Color.Default"
                ValueChanged="OnIsOpenedChanged"
                T="bool">
                Відчинений
            </MudCheckBox>
            <MudDatePicker
                Date="@StoreSearchParameters.SetupDate"
                Label="Дата відкриття"
                DateChanged="OnOpeningDateChanged"/>
        </MudForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Назад</MudButton>
        <MudButton Disabled=" string.IsNullOrEmpty(StoreSearchParameters.Name) && string.IsNullOrEmpty(StoreSearchParameters.Address) && StoreSearchParameters.SetupDate == null"
                   OnClick="ResetSearchParameters">
            Очистити
        </MudButton>
        <MudButton OnClick="Search">Пошук</MudButton>

    </DialogActions>
</MudDialog>